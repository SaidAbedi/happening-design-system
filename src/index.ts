/**
 * @my-scope/design-system
 *
 * Root export - platform-agnostic tokens only.
 * This is safe to import from any environment.
 *
 * For platform-specific themes, use:
 * - '@my-scope/design-system/web' for React web apps
 * - '@my-scope/design-system/native' for React Native apps
 * - '@my-scope/design-system/tokens' for tokens only (same as root)
 */

export * from './tokens';
