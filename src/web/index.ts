/**
 * Web Theme Adapter
 *
 * Import from '@my-scope/design-system/web'
 */

export {
  createWebTheme,
  generateCSSVariables,
  generateCSSVariablesString,
  type WebTheme,
  type WebThemeOptions,
} from './createWebTheme';

// Re-export tokens for convenience (optional)
export * from '../tokens';
